# Example project for the BSP.
# BSP and kni_ui are resolved via main/idf_component.yml local paths.

cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(display_lvgl_demo)
